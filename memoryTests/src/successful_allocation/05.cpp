#include <ctime>

const int size = (int) 1e6;

int a[size], b[size], c[size], d[size], e[size], f[size], g[size], h[size], i[size], k[size];

int main()
{
	clock_t goal = 500 + clock();

	while (goal > clock());
    
	return 0;
}